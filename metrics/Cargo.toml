[package]
name = "metrics"
authors = ["Jakub Zajkowski <jakub@casper.network>"]
version = "1.0.5"
edition = "2024"
readme = "README.md"
description = "Lib aggregating metrics for casper-sidecar"
license-file = "../LICENSE"
documentation = "README.md"
homepage = "https://github.com/casper-network/casper-sidecar/"
repository = "https://github.com/casper-network/casper-sidecar/"

[dependencies]
prometheus = { version = "0.14", default-features = false, features = [
    "process",
] }

[features]
additional-metrics = []
