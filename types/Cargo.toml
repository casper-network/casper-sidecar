[package]
name = "casper-event-types"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-execution-engine = { git = "https://github.com/casper-network/casper-node", branch = "dev", optional = true }
casper-hashing = { git = "https://github.com/casper-network/casper-node", branch = "dev", optional = true }
casper-node = { git = "https://github.com/casper-network/casper-node", branch = "dev"}
casper-types = { git = "https://github.com/casper-network/casper-node", branch = "dev"}
serde = { version = "1.0", features = ["derive"]}
serde_json = "1.0"
rand = { version = "0.8.5", optional = true }
thiserror = "1.0.39"

[features]
sse-data-testing = ["rand", "casper-node/testing", "casper-execution-engine", "casper-hashing"]
