[workspace]
resolver = "3"
members = [
    "event_sidecar",
    "json_rpc",
    "listener",
    "metrics",
    "rpc_sidecar",
    "sidecar",
    "types",
]

[workspace.dependencies]
anyhow = "1"
async-stream = "0.3"
async-trait = "0.1"
casper-types = { version = "8", features = [
    "json-schema",
    "std",
] }
casper-binary-port = { version = "2" }
casper-event-listener = { path = "./listener", version = "3" }
casper-event-sidecar = { path = "./event_sidecar", version = "3" }
casper-event-types = { path = "./types", version = "3" }
casper-rpc-sidecar = { path = "./rpc_sidecar", version = "3" }
casper-json-rpc = { path = "./json_rpc", version = "3" }
datasize = "0.2"
derive-new = "0.7"
futures = "0"
futures-util = "0.3"
governor = { version = "0.10", default-features = false, features = [
    "dashmap",
    "std",
] }
itertools = "0.14"
jsonschema = "0.33"
metrics = { path = "./metrics", version = "3" }
rand = "0.8"
serde = { version = "1", default-features = false }
thiserror = "2"
tokio = "1"
toml = "0.9"
tracing = { version = "0", default-features = false }
tracing-subscriber = "0"
warp = { version = "0.3" }

[patch.crates-io]
casper-binary-port = { git = "https://github.com/casper-network/casper-node", branch = "dev" }
casper-types = { git = "https://github.com/casper-network/casper-node", branch = "dev" }